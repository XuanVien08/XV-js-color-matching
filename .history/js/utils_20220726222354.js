export const getRandomColorPairs = (count) => {
  // receive count --> return count * 2 random colors
  // using lib: https://github.com/davidmerfield/randomColor

  const colorList = []

  const randomColor = [red, green, blue, yellow, orange, purple, monochrome]

  return colorList
}
